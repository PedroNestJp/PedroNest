let produto = "Maça"
let quantidadeDoProduto = 0
let quantidadeFaltandoDoParaPromocao = 5 - quantidadeDoProduto
let quantidadeRestanteDeMacas = 

function validaFruta(produto){
if (produto === "Maça") {
    return `você escolheu a fruta ${produto}`
}}

function validaPromo(quantidadeDoProduto){
    if (quantidadeDoProduto >= 5 ){
        return "Você recebeu uma promoção, o valor do produto sairá por R$5.00"
    }
    else (quantidadeDoProduto <=5)  
        return (`se você adicionar mais ${quantidadeFaltandoDoParaPromocao} o valor sairá por : R$5.00 `)
     }
     
console.log(quantidadeDoProduto(4))
console.log ()
