let estados = require('./estados.json');

for (let contador =0; contador <= estados.estados.length; contador++) {
    console.log(contador)
    console.log(estados.estados,[contador]);

if (estados.estados[contador].nome === 'Paraíba') {
    console.log(estados.estados,[contador]);
}

S
}
    


//estados.estados.forEach((estados,contador) => {
    //console.log(contador)
    //console.log(estados.cidades)
        
        
    
//});


