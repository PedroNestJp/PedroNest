// const filmes = [
//     'Até o último homem',
//     'Ultimato',
//     'Spider Man',
//     'Senhor dos Anéis'
// ]
//console.log(filmes[1])

//for (i = 0; i <= filmes.length; i++)
    //console.log(i)
    const numeros = [ 10, 5, 8, 20, 13 ]

    const numerosmult2 = numeros.map((numeros) => numeros * 2)
    console.log(numeros)
    console.log(numerosmult2)
    //Criar um Map para pegar o array de numeros
    // e multiplicar * 2
    // const numerosMultiplicadosPorDois = numeros.map((numero) => {
    //     return numero * 2
    // })
